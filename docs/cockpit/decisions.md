---
title: "Beslis-register — alle genomen productbeslissingen (index)"
type: index
status: active
---

# Beslis-register — alle genomen productbeslissingen (index)

> **Dit is de canonieke, browsbare index van "welke richtingsbeslissingen zijn genomen
> en wat was de uitkomst".** Eén regel per beslissing: datum, de vraag, de uitkomst, en
> een link naar het diepere beslisdocument + de kanban-kaart.
>
> **Dit register is een index, geen archief.** De redenering, de trade-offs en de
> alternatieven staan in het gelinkte document — die worden hier **niet** gedupliceerd.
> Verandert een beslissing, dan wordt het bron-document bijgewerkt en krijgt het register
> een nieuwe regel (de oude blijft staan, met een `↩︎ herzien door`-verwijzing).

**Waarom dit bestaat.** Een beslissing werd tot 2026-07-15 op drie losse plekken
vastgelegd — de kaart-Done-summary, een `*-decision.md`, soms een memory-file — zonder
index. De Done-kolom is een firehose (je kunt er "alle beslissingen" niet uit filteren)
en [`kanban-followups.md`](./kanban-followups.md) dekt alléén de
"upstream-deliberately-NOT-adopted"-klasse. Een sessie die vroeg *"doen we same-vendor
multi-account?"* moest toevallig op de juiste spike-doc stuiten. Dit register is die
canonieke plek.

**Voor je een beslissing heropent:** zoek 'm hier eerst op. Staat er een uitkomst, dan is
de vraag beslist — heropenen kan, maar dan met een expliciete weerlegging tegen het
gelinkte document (zie [`reopen-completed-decision-analysis.md`](./reopen-completed-decision-analysis.md)),
niet omdat de beslissing onvindbaar was.

## Het register (nieuwste eerst)

| Datum | Vraag | Uitkomst | Document | Kaart |
|---|---|---|---|---|
| 2026-08-07 | Na een `uvicorn --reload` (of elke backend-restart) ziet de MCP-client van een gedispatchte agent zijn `session_id` nog, maar de server heeft de SSE-sessie-state (`SseServerTransport._read_stream_writers`) al weggegooid. Elke tool-call faalt met `MCP error -32602: Invalid request parameters` — misleidend, want de *params* zijn goed, de *sessie* is weg. Fix aan server-kant (state-survival), client-kant (SSE-reconnect) of minimaal een eerlijke foutmelding? | **Minimaal — session-aware wrapper rond `sse.handle_post_message` die 410 Gone + JSON `{error: session_not_found, reconnect_required: true}` teruggeeft.** Geen state-survival (broker-kosten + lost multi-worker SSE-fundament niet op) en geen client-fix (we bezitten Claude Code niet). De agent herkent nu het verschil tussen "sessie weg" en "params kloppen niet", en Claude Code's SSE-client heropent de stream automatisch bij een 410. Nieuwe module `app/kanban/mcp_transport.py` + 5 tests in `test_kanban_mcp_session_410.py`; `main.py` mount de wrapper in plaats van `kanban_mcp.sse_app()`. | [`kanban-mcp-session-410-decision.md`](./kanban-mcp-session-410-decision.md) | `ae19ced1…` |
| 2026-08-06 | De code-scanning-tab telt 243 open alerts die geen 243 problemen zijn — `py/path-injection` alleen al is 224 hits over ~28 bestanden. Welk dreigingsmodel past bij een lokaal single-user controlepaneel, hoe beoordelen we de 8 regelgroepen, en waar landt een by-design-uitspraak zodat geen 228 UI-dismissals ontstaan? | **Eén dreigingsmodel, één regel-tabel, één commitbare policy.** 243 alerts groeperen tot 8 regels. 5 by-design (233 alerts), 1 real (skills_registry `source`), 1 noise (coverage-sorter), 1 mechanical. Geen 228 UI-dismissals — CodeQL advanced setup commit de by-design-policy. | [`security-scanning-decision.md`](./security-scanning-decision.md) | `beace361…` |
| 2026-08-05 | Zes CI-stappen staan op `continue-on-error: true` en hun bevindingen bestaan alleen in een log dat niemand leest. Welke gates moeten hard falen, en hoe komt een bevinding automatisch terug op het kanban-bord? | **Drie CI-gates blijven advisory op korte termijn, één gate wordt hard, één gate verdwijnt; semgrep krijgt SARIF-upload; CI-naar-bord-terugkoppeling loopt via een mens-getriggerde `file_ci_drift_cards.py` naast de bestaande drift-report; de direct-mode-route krijgt een aparte auto-fix-workflow.** | [`ci-gate-decision.md`](./ci-gate-decision.md) | `4f56f169…` |
| 2026-08-05 | De 39 `scripts/test_*.sh`-harnesses draaien in geen enkele CI-workflow. Welke klassen horen in CI (en hoe isoleren we hun externe state), welke blijven lokaal, en hoe harden we de gate zonder de huidige twee rode runs als eerste indruk te laten staan? | **Drie CI-tiers + één lokale klasse; wallclock-asserts blijven met verhoogde drempel; één nieuwe parallelle CI-job `bash-tests`; de twee rode harnesses krijgen elk een fix-kaart vóór de gate hard wordt.** | [`bash-test-ci-tiering-decision.md`](./bash-test-ci-tiering-decision.md) | `2fe2e4d2` |
| 2026-08-04 | Er zijn twee manieren om "doe dit ding op tijdstip T" te zeggen: een kanban-kaart met een toekomstige `scheduled_at` (die dan weken zichtbaar in Backlog wacht) en de scheduled-messages-feature (APScheduler + cron + `tmux send-keys` in een bestaande sessie). Welke van de twee blijft, en moet een geplande kaart pas ontstaan op het moment dat ze nodig is? | **Kanban blijft de enige uitvoeringsweg; de klok wordt een trigger die de kaart aanmaakt.** Een terugkerende taak krijgt een server-side cron-trigger waarvan de actie `create_card` is — hetzelfde patroon dat de GitHub-webhook al gebruikt (`webhooks/router.py:1-7`) — zodat de kaart pas bestaat op de ochtend dat ze gedaan moet worden, en de chain-of-one-shots uit de skill-prompt vervalt. De tmux-injectieroute (`delivery.py` + `crud.py`, nul importeurs buiten het package) gaat uit; het gedeelde sessie-substraat eronder (`session_registry`, `tmux_inject`, hook-ingest, auto-resume) en de hook-/auto-resume-endpoints blijven, want die dragen kanban-dispatch en Agent Mail. Doorslag gaf de meting: B stond vijf weken scherp met 1 rij, `last_fired_at=NULL` en 0 delivery-attempts, en miste zijn enige maandag structureel (in-memory jobstore ⇒ geen inhaal na een restart), terwijl A's per-tick `is_due()` een gemiste due-tijd wél inhaalt. ↩︎ herziet `recurring-cadence-proposal.md` §2/§6. | [`scheduled-trigger-consolidatie-decision.md`](./scheduled-trigger-consolidatie-decision.md) | `0767c57a` |
| 2026-08-04 | De §8-implementatie van `c31333bf…` schreef de RTK-wrapper + een gemuteerde `settings.json` in de dispatch-worktree. Dat breekt de ship-gate: `git status` toont `?? .claude/hooks/` en ` M .claude/settings.json` vanaf de eerste seconde van élke saver-sessie, en de recovery-stap (`git add -A && git commit`) commit de hook naar `master` waardoor RTK board-wide aan komt te staan zonder lane-vlag en zonder dat de kill-switch het kan terugdraaien. Hoe houden we de opbrengst op shippende lanes zonder die git-pollutie? | **Wrapper naar de RTK-cache, hook-config naar `.claude/settings.local.json`.** De wrapper (`<cache>/<version>/hooks/rtk-cockpit-rewrite-wrapper.sh`) leeft nu in de per-versie cache parallel aan de upstream `rtk-rewrite.sh` die hij met `$(dirname "$0")/rtk-rewrite.sh` aanroept — één kopie per host, gedeeld door alle worktrees, geen muteert op de worktree. De hook-config gaat naar `.claude/settings.local.json` i.p.v. de tracked `.claude/settings.json`; Claude Code's default gitignore dekt `**/.claude/settings.local.json` (`~/.config/git/ignore:1`) waardoor het bestand volledig onzichtbaar is voor `git`. De PreToolUse-command referencet het absolute pad naar de wrapper zodat hij werkt vanuit elke worktree. Effect gemeten in een echte git-worktree (`git init -b main` + `git worktree add`, niet `tmp_path`): `git status --porcelain` leeg na installatie. Twee nieuwe regressietests (`test_active_branch_leaves_worktree_git_status_clean`, `test_active_branch_preserves_tracked_settings_json`) dekken respectievelijk de kale installatie en het scenario waarin een operator al een tracked `.claude/settings.json` heeft; de bestaande `_read_settings`/`_write_settings`-helpers zijn bijgewerkt om naar `settings.local.json` te wijzen. **Ongewijzigd:** publieke `token_saver.py`-surface (`maybe_install` / `is_board_enabled` / `set_board_enabled` / `post_note` / `write_rtk_settings_into_worktree`), per-lane vlag, board-wide kill-switch, fail-open, activity-feed note + 60-s-dedup, `RTK_TELEMETRY=off`, wrapper-body (zelfde `grep` carve-out + `git diff` bypass-list; alleen het bestemmingspad verandert), lockstep-meet-harnas en de §8 N=2-conclusie. **Geen promotion naar default-on.** ↩︎ afgeleid van `token-saver-mechanismen-decision.md` §8 + de reviewer's product-effect-bewijs uit IMPEDIMENT ronde 4. | [`token-saver-mechanismen-decision.md` §8](./token-saver-mechanismen-decision.md) | `c31333bf…` |
| 2026-08-04 | De dispatcher kan output-tokens besparen door de MIT-gelicentieerde upstream plugins `JuliusBrussee/caveman` (terse-speak) en `DietrichGebert/ponytail` (lazy senior dev, YAGNI-first) als systeemprompt-slices te injecteren — dat concurreert met "iedere sessie krijgt het automatisch" en botst frontaal met acceptance-criteria-zwaar werk (engineer) of analysedoc-as-deliverable (analyst leaf-spike). Hoe geven we operators de knop zonder de kaart aan te zetten op lanes waar de slice het artefact zelf kwaad doet? | Twee onafhankelijke per-lane schakelaars + één board-wide kill-switch; default uit. `kanban_columns.caveman_enabled` + `kanban_columns.ponytail_enabled` (INTEGER 0/1, default 0) staan het per-lane opt-in; één `prompt_injector:<project_key>`-rij in `KanbanMeta` (`"1"` engages) forceert BOTH uit ongeacht kolom-vlaggen — fail-closed, hot-path read, geen herstart. De slice zit louter in `build_card_prompt` tussen persona en kaarttekst (kaarttekst + ship-recipe + impediment/revisit-secties onaangetast); de resolver is puur zodat de cache-prefix per sessie stabiel blijft. Operator-facing oppervlak: `GET/POST /api/v1/kanban/prompt-injector` (kill-switch), `PATCH /api/v1/kanban/columns/{id}` met `caveman_enabled`/`ponytail_enabled` in `ColumnUpdate`, frontend-toggles in `ColumnSettingsDialog.tsx`. **Meting 2026-08-09 verbatim slice** (`scripts/measure-token-saver.sh with-injector`, N=2 trials per variant op de golden-task; artefacten onder `/home/vdvgu/.cache/cockpit-measure-token-saver/20260809T223000Z-verbatim-slice/`): baseline `input=49096 / output=1530 / cache_read=238208`, with-injector `input=51468 / output=978 / cache_read=225664`, **Δ input +4.8%, Δ output −36.1%, Δ cache_read −5.3%**, `pass_tests=2/2 = 2/2`. De eerdere 2026-08-05-proxy-cijfers (`Δ input −57%, Δ output −65%, Δ cache_read −51%`) overstate de effecten vanwege de ~110-byte prelude/tail — die verschuift de cache-key synthetisch; de verbatim resolver (`tests/test_prompt_injectors.py::test_resolver_returns_byte_stable_output_for_same_inputs`) is byte-stabiel en laat `cache_read` in een multi-dispatch-sessie wel degelijk renderen. Research-/sweep-lanes geschikt (de upstream Caveman-§"Auto-Clarity" carve-out houdt security/irreversible-zinnen buiten compressie); analysis-leaf-lane niet geschikt uit voorzorg (de Caveman-§"Boundaries" carve-out zegt expliciet "Persisted outside chat: write normal prose" — verbatim opgenomen, maar het risico is een **verwachting** zolang geen echte analyse-leaf-meting is gedaan; een vervolgkaart volgt). Prompt-tekst + commit-pin + MIT-attributie liggen vast in `backend/app/kanban/prompt_injectors.py`. 25 tests in `backend/tests/test_prompt_injectors.py` + 7 in `backend/tests/test_prompt_injector_api.py` dekken default-uit, onafhankelijke vlaggen, kill-switch (helper + API), byte-stabiliteit, activity-feed-audit, schema-velden en de "alleen de systeemprompt-laag"-belofte. ↩︎ overrulet `9router-integratie-analyse.md` §4.2; herzieningen aan deze rij worden gelogd in `prompt-injectors-decision.md` §"Eerste kwaliteitsmeting per lane-type" met de artefact-paden onder `/home/vdvgu/.cache/cockpit-measure-token-saver/`. | [`prompt-injectors-decision.md`](./prompt-injectors-decision.md) | `d0446fd8…`, ↩︎ verbatim-herziening via `5934b954…` |
| 2026-07-29 | De `intake`-kolom + Promote-knop (optie 2 uit `product-inceptie-pipeline.md` §4, gebouwd 2026-07-11) zet een kaart op het meta-project tussen een app-idee en het echte project. Verdient die tussenkaart zijn bestaan? | **Nee — optie 3, kaartloze geboorte.** De route is in zijn hele levensduur **nul keer** voor een echt idee gebruikt: precies één kaart stond ooit in `intake` (`eae625cc…`, een test-fixture) en géén enkele kaart droeg een `Promoted to project`-spoor. Dat is geen adoptieprobleem maar een teken dat de tussenstap op de verkeerde plek zit: de kaart leeft op het meta-project terwijl hij over een ánder project gaat (hetzelfde kip-en-ei-bezwaar dat §2.3 bij optie 1 al noteerde, verplaatst in plaats van opgelost), de Promote-klik is een gate zónder beslissing (het ontwerp is op dat moment al sectie voor sectie goedgekeurd), en plan-markdown zou in twee bronnen tegelijk leven (deliverable + repo). Nieuwe vorm: een interactief `new-app`-interview (`brainstorming` + `writing-plans`, incrementeel weggeschreven naar een durabele scratch-map buiten elke git-repo, `--resume`-baar) roept `create_project_from_interview` aan; spec + plan landen als `docs/specs/…` + `docs/plans/…` in het nieuwe repo **vóór** de eerste commit, en de eerste Backlog-kaart draagt `metadata["spec_doc"]` — dat vervangt de `plan_ref`-traceability en wijst naar een bestand in de repo zelf i.p.v. naar een kaart op een ander bord. Alles atomisch (filesystem + `Project`-rij + autodispatch-meta + kaart rollen samen terug). **Ongewijzigd:** `WORK_TYPES` blijft vierwaardig (geen `work_type="intake"`), `intake_kind` komt er niet (de consument verdween mét de kolom), de toolkeuze uit `intake-authoring-flow-decision.md` §4.1 en de mens-in-de-lus-vorm uit §4.2 (native interactief, nooit `report_impediment` voor de dialoog). Sloop uitgevoerd in `d0531c12…` nadat route + skill werkten. **Heropenen** bij: een echte behoefte aan portfolio-triage van meerdere ideeën vóór er één gebouwd wordt (ander probleem, eigen vorm), of als de knoploze flow onbruikbaar blijkt — een UI-ingang op `create_project_from_interview` is additief en vereist deze beslissing niet te heropenen. | [`kaartloze-app-inceptie-decision.md`](./kaartloze-app-inceptie-decision.md) | `b9e6365a…`, `1fa1b693…`, `d0531c12…` |
| 2026-08-01 | Cadans-trigger-kaarten (`recurring-cadence-proposal.md` §4) fileden hun bevindingen bewust standalone, zonder `parent_card_id` naar de trigger. De drie bestaande uitkomsten (`decomposed`/`not_feasible`/`no_action_needed`) passen daar niet op: `decomposed` weigert met `no_children`, parent-parking plaatst de trigger in `Awaiting Subtasks` als de kinderen toch `parent_card_id` krijgen, en `no_action_needed` labelt een productieve run oneerlijk. Kies een vierde uitkomstwaarde of maak parent-parking conditief op een recurring-label? | **Vierde uitkomst `filed_standalone`** met DB-sterke verificatie via `card.metadata.filed_card_ids` (lijst van kind-kaart-ids in dezelfde `project_key`; geen FK-vereiste op `parent_card_id`). Route 2 (parent-parking-skip op `recurring`) lost maar één van de twee faalklassen op en snijdt een tweede carve-out in een doelbewuste analyse-levenscyclus-beslissing. De nieuwe waarde blijft binnen de bestaande `is_analyst_leaf_spike`-gating (backwards-compatible voor niet-analyse-kaarten) en de `**Outcome:**`-comment blijft de canonieke neerslag; geen nieuw label toegevoegd omdat `filed_standalone` geen uitkomst-taxonomie is maar een card-relationshipvorm. `scripts/check-analysis-outcomes.sh` accepteert `**Outcome:** filed_standalone` als vierde witness. ↩︎ afgeleid van `analysis-outcome-contract-decision.md`. | [`analysis-outcome-contract-decision.md` §9](./analysis-outcome-contract-decision.md) | `5770d3db` |
| 2026-07-28 | De trigger-poort uit `acp-transport-trigger-gate.md` is opengegaan — de `open-code`-CLI bracht 37 kaarten naar Done. Is OpenCode's ACP-adapter volwassen genoeg om Cockpit's transport voor die CLI te dragen, afgewogen tegen zijn native modus? | **GO op een ACP-backed `SpawnTransport`-sibling voor `open-code`.** Gemeten tegen OpenCode 1.18.8: `opencode acp` is een first-party ACP-server die een volledige `initialize` → `session/new` → `session/prompt`-cyclus over stdio-pipes zonder TTY aflegt en het gevraagde bestand daadwerkelijk schrijft; `session/request_permission` vuurt als getypeerde gating-haak (facet D) mét unified diff, mits de spawn een permission-config meekrijgt die niet op auto-allow staat. De native route (`opencode run --format json`) leverde in vier varianten — inclusief plain `run` en met gesloten stdin — 0 bytes binnen de timeout, precies in de contextvorm waarin een `SpawnTransport` draait; die observatie is reproduceerbaar maar bewust niet root-caused. Het bestaande `structured_events.py`-model draagt de gemeten ACP-events ongewijzigd (4 van 6 varianten gemeten kloppend), met twee gaten: `usage_update` en `available_commands_update`. NO-GO blijft staan op "één ACP-client dekt N vendors" — dat vraagt een tweede binary en een tweede meting. De claude-code stream-json-transport blijft ongewijzigd; ACP komt ernaast, niet ervoor in de plaats. | [`acp-transport-opencode-go-nogo.md`](./acp-transport-opencode-go-nogo.md) | `a4a091fa` |
| 2026-07-28 | Bordkaarten zijn "nog steeds suboptimaal — kan niet alles lezen en nog veel te vaak scrollen", ná een eerdere ronde die de titel juist op twee regels afkapte. Geven we de titel ruimte binnen de kaart, of gaat het bord naar een heel andere weergave (tabel/lijst)? | **De bordkaart wordt een leesoppervlak voor zijn titel en een scanoppervlak voor de rest.** De titel krijgt de volle kaartbreedte en vijf regels (was twee, náást de statusbadges); de metadata zakt naar één regel die afkapt met een fade in plaats van naar drie regels te wrappen; lege kolommen klappen zichzelf in tot een 40px-rail en elke kolom is handmatig in/uit te klappen (bewaard in `localStorage`). Een tabel-/lijstweergave van het bord is afgewezen, en de kaart-modal is bewust buiten scope gebleven — die loopt via `kaart-modal-leesfirst-decision.md`. Gemeten op het echte bord (1440×900): horizontaal schuiven 728px → 56px, afgekapte titels 38/49 → 8/50, mediane kaarthoogte 144px → 115px. De clamp op vijf regels is gekozen op de knik van de gemeten curve: vier regels kapt 12/40 titels af, vijf kapt 6/40 af bij exact dezelfde mediane hoogte, zes kost een zichtbare kaart voor drie titels. Eerlijk gebleven trade-off: verticaal scrollen in Backlog verbetert op 1440×900 niet (4 zichtbare kaarten vóór én ná) — de winst is dat die schermvulling nu leesbare titels zijn in plaats van chips, plus een inklap-knop die er eerst niet was. | [`board-card-layout-decision.md`](./board-card-layout-decision.md) | `1fafd87c` |
| 2026-07-27 | LiteLLM draait als sidecar naast Cockpit. Wie beheert zijn lifecycle, wat gebeurt er als de proxy down is (fail-open terugvallen op Anthropic, of fail-closed stoppen), welke dispatch-lanes mogen erdoor, hoe worden keys uitgegeven, en pinnen we de versie? | De LiteLLM-sidecar draait als **opt-in, gesuperviseerde derde service onder `cockpit.sh`**; dispatch is **fail-closed** op een dode proxy en de terugval gebeurt één laag hoger — in de pool-router vóór de spawn — in plaats van in een error-handler ná de fout. Fail-open is afgewezen omdat het de intentie inverteert in beide inzet-modi (bij een prijs-lane besteedt het juist het abonnement dat gespaard moest worden; bij een bereik-lane draait de kaart op een ander model dan gevraagd) en omdat het het `provider`-veld op de kaart laat liegen — bovenop de `betrouwbaarheid="onbekend"` die `390756e6…` al bewust koos. Fail-closed kost nul regels code: het bestaande spawn-faalpad geeft de claim vrij, telt door naar `MAX_DISPATCH_FAILURES = 3` en zet de kaart met de echte fout in Impediment. Verder: doctor krijgt een conditionele, advisory WARN-check die `check-litellm-hardening.sh` hergebruikt; toegestane lanes zijn alleen een expliciete pin (`column_overrides`/kaart-provider) en de **laatste** pool-entry (vangnet) — `column.default_provider` en de globale override zijn verboden omdat die de sidecar verplicht maken; één master key in de project-scoped SecretStore i.p.v. per-sessie virtuele keys (die vereisen een LiteLLM-DB, gemeten, en attributie heeft met de response-headers een goedkopere bron); en `litellm==1.93.0` + `prisma` exact gepind, doorslaggevend omdat een upstream-hernoeming van een config-flag de hardening-check **stil hol** maakt (hij vindt de flag niet en meldt PASS). | [`litellm-sidecar-lifecycle-decision.md`](./litellm-sidecar-lifecycle-decision.md) | `2bfefe22` |
| 2026-07-25 | De kaart-modal geeft "heel wat schuifbalken" en mengt lezen, bedienen en telemetrie. Wordt dit opgelost binnen de modal (lees-first herindeling) of door de diepe content naar een eigen kaart-pagina `/kanban/card/:id` te verhuizen? | **Richting A — lees-first modal.** De kaart-modal krijgt één scroll-container en drie zichtbaarheids-lagen (actie-vereist → lezen → operator/telemetrie ingeklapt); richting B (`/kanban/card/:id`) wordt **niet** gebouwd. Doorslaggevend is niet de kostprijs maar dat **B de gemelde klacht niet oplost**: de geneste schuifbalken komen uit hoogte-caps op de binnen-content (`CardRunTab.tsx:110,192`, `CardLedgerTab.tsx:127`, `MarkdownPreviewToggle.tsx:31,39`), en die reizen ongewijzigd mee naar een pagina die zelf óók scrollt — B adresseert alleen de tweede deeloorzaak ("te veel op één oppervlak"). B blijft goedkoop mogelijk (de diepe tabs zijn al zelfstandige componenten) en is bewust niet als speculatieve kaart gefiled. Hybride afgewezen: twee weergaven van dezelfde kaart plus de vraag welke de `?card=`-deeplink opent. | [`kaart-modal-leesfirst-decision.md`](./kaart-modal-leesfirst-decision.md) | `624f7718` |
| 2026-07-23 | De fase-2 spillover is dode code (geen pool geconfigureerd), maar een pool aanzetten overschrijft stilzwijgend `column.default_provider` van élke kolom en sloopt daarmee de bewuste per-persona-verdeling. Welke vorm krijgt spillover dan — pool per kolom, kolom-default als impliciete eerste pool-entry, of spillover blijft uit? | **GO op vorm B, uitgebreid tot per-kolom staarten — de pool wordt een spillover-*keten* in plaats van een routing-*pin*, met `column.default_provider` als impliciete eerste entry.** De override-claim is **bevestigd** op de huidige code: `resolve_effective_provider_and_model` haalt de pool project-breed op (`dispatch.py:1233`) zonder de kolom te kennen, en `pool_choice.provider` (`:1240`) staat in dezelfde `or`-keten bóven `column_default_provider` (`:1242`); de enige bestaande ontsnapping is per-CLI, niet per kolom (`subscription_pool.py:229-231`). Nieuwe effectieve keten per kolom K: `[K's default] ++ [pool minus K's default]` — `analyst` start dus op Anthropic/opus en `engineer` op MiniMax precies zoals nu, en de pool bepaalt alleen nog waar ze *heen* mogen als die kop gepauzeerd of boven drempel is. **Waarom niet vorm A (per-kolom vervangende pool):** die dwingt de operator de kolom-default te dupliceren als eerste pool-entry, wat een nieuwe stille koppeling tussen twee configuratieplekken introduceert — precies de bug die deze analyse-lijn oplost — en verdrievoudigt de dialoog die al als "te groot" was aangemerkt. **Waarom de pool-als-pin redundant is:** `global_override` doet hetzelfde, wint ervan, én schakelt de pool uit terwijl hij aan staat (`dispatch.py:1235`). **Nul migratie-risico, en dat vervalt:** gemeten 2026-07-23 staat er geen enkele `subscription_pool:`-rij in de board-DB, dus de semantiek herdefiniëren kost vandaag niets en is breaking zodra iemand een pool aanzet. **Nuloptie afgewezen** (65 limiet-events in 8 dagen blijven volledig op de reset-klok), maar met een expliciet erkende kost: spillover ruilt "5,7 h wachten" in voor "nu verder op een zwakker model" — een **ongemeten inschatting**, en de reden dat de staart opt-in, leeg-by-default en per kolom instelbaar wordt. Twee latente defects meegefiled die vandaag al via `global_override` bereikbaar zijn: het kolom-model-alias `opus` lekt mee naar een MiniMax-spawn (`dispatch.py:1136-1137` gate't alleen de persona-fallback) en het reactieve limiet-pad resolvet de gelimiteerde provider pool-blind (`dispatch.py:3982` → `_provider_for_card`). 5 vervolgkaarten. **Heropenen** bij: een tweede CLI die dispatcht (de per-CLI-as wordt dan een echte tweede dimensie), of een kwaliteitsmeting per provider per persona die §6 van inschatting naar meting tilt. | [`spillover-per-kolom-decision.md`](./spillover-per-kolom-decision.md) | `2688bf80…` |
| 2026-07-24 | Token-saver (RTK 0.43.0) per-lane geïntegreerd met board-wide kill-switch en fail-open op élke stap — vóór we de feature default aan durven zetten. | ✅ **Geïntegreerd, opt-in, default uit, gemeten via lockstep-harness.** RTK is een Claude-Code `PreToolUse`-hook op `Bash` (geverifieerd: `git status` → `rtk git status`) die commando's vóór uitvoering rewrite — geen `tool_result`-compressie, geen API-rewrite, **cache-veilig by construction** (de hook leeft in `.claude/settings.json`, raakt het prompt-array niet). Integratieoppervlak: per-lane `KanbanColumn.token_saver_enabled` (kolom-migratie additief, fail-safe in `_ensure_column_table`), board-wide runtime kill-switch via `KanbanMeta` row `token_saver:<project_key>` (Pydantic-validated), nieuwe `token_saver.py` met publieke surface `maybe_install` / `is_board_enabled` / `set_board_enabled` / `post_note` / `write_rtk_settings_into_worktree` (de sync-vorm van `write_rtk_settings_into_worktree` is exact dezelfde code path die de dispatch-helper gebruikt — `apply_real_saver` in de harness spreekt 'm aan, geen tweede JSON-merge-pad). Dispatch-integratie via sync-bridge `_install_rtk_for_dispatch` vanuit `make_worktree_transport`; `RTK_TELEMETRY=off` expliciet in spawn-env. Activity-feed observability via `post_note` met 60-s-dedup, dus één `**Note:** Token saver activated: RTK 0.43.0` per dispatch. Frontend: toggle in `ColumnSettingsDialog`, type-veilig via `KanbanColumn.token_saver_enabled: boolean`, API-client `getTokenSaver` / `setTokenSaver`. Tests: 16 unit (`test_token_saver.py`) + 4 kolom-migratie (`test_token_saver_column.py`) + 4 API (`test_token_saver_api.py`), groen. **Gemeten effect via `scripts/measure-token-saver.sh compare`:** `real-saver`-run faalde closed (RTK-binary niet op deze host geïnstalleerd; `apply_real_saver` gaf exit 12 "rtk version mismatch (got '', need 0.43.x)" — geen stille fallback naar de prompt-mutatie-proxy, precies zoals `decisions.md §6b67df66` eiste). De `with-saver`-proxy-ondergrens herhaalt de `6b67df66`-meting: `cache_read` daalt −42,4% op N=1, `input_tokens` −0,5% (prefix-cache-gemaskeerd), `output_tokens` −14,3%, `pass_diff=1` (agent paste juiste fix toe), `pass_tests=0` = **scoring-infra-failure** (worktree-venv ontbreekt, geen saver-regressie). **Conclusie:** de integratie staat, `default off`-criterion staat (de feature is opt-in, geen operator wordt geraakt), en de proxy-ondergrens herbevestigt `6b67df66`'s `cache_read`-richting — precies wat `c31333bf…` vroeg. **Heropenen** bij: RTK-binary geïnstalleerd + `real-saver`-run met `pass_tests=1` en `pass_diff=1` die nul of negatieve token-delta toont (dan is de feature niet de moeite, vervolg-kaart onderzoekt waarom), of Anthropic wijzigt de `cache_read`-boekhouding zodat de proxy-eis aan `Bash` verandert. | [`2026-07-24-token-saver-integration-design.md`](../superpowers/specs/2026-07-24-token-saver-integration-design.md) + `token-saver-mechanismen-decision.md` §8 | `c31333bf…` |
| 2026-07-25 | De activity-feed-claim uit de 2026-07-24-rij was loos: `post_note` bestond als helper en was unit-getest, maar de dispatch-bridge riep 'm niet aan — een reviewer flagde dat en de kaart ging weer open. Hoe herstellen we het zonder de 2026-07-24-fix te herzien? | ✅ **Bridge-refactor + vier tests, geen surface-wijziging.** De sync-bridge `_install_rtk_for_dispatch` is gesplitst in een dunne `asyncio.run`-wrapper en een async kern `_install_rtk_for_dispatch_async` die dezelfde private kanban-engine opent, `maybe_install` aanroept, en voor de **active** + **failed (fail-open)** paden in dezelfde sessie `post_note` aanroept met de juiste tekst (`activated: <reason>` of `fail-open: <reason>`) en commit. De **inactive**-pad (per-lane off, board-wide off) post niets — die loopt op élke Backlog-dispatch en zou de activity-feed overstromen. De bridge geeft nu `(status, reason)` terug in plaats van alleen `status`, zodat de `RTK_TELEMETRY=off`-beslissing en de activity-feed-tekst aan dezelfde bron gekoppeld zijn — geen tweede afleiding in de caller. **Vier tests in `test_token_saver.py`** — drie voor de async-kern (`test_dispatch_bridge_posts_activated_note_on_active`, `test_dispatch_bridge_posts_fail_open_note_on_missing_binary`, `test_dispatch_bridge_silent_on_inactive_path`) die monkeypatchen `settings.kanban_database_url` naar de test-DB en de **async kern** direct aanroepen (omdat `asyncio.run` vanuit een pytest-asyncio test niet mag), plus één sync-bridge test (`test_sync_bridge_posts_activated_note_on_real_dispatch`) die de **sync wrapper in een worker thread** draait en daarmee bewijst dat de `asyncio.run`-grens de note niet sluit — precies de test die een FCR-ronder expliciet eiste. Geen surface-wijziging in `token_saver.py`, geen frontend-touch, geen meet-harnas-wijziging. Tests: 20/20 unit + 4/4 kolom-migratie + 4/4 API groen. **Heropenen** bij: een race tussen twee gelijktijdige dispatches op dezelfde card waardoor de HLC-tick van `_clock_for` de dedup niet strak genoeg serialiseert (dubbele notes), of een operator wil de note-tekst in de UI op de card zelf zien (nu alleen in de activity-feed-component). ↩︎ herzien door de rij direct hieronder (2026-07-25, productie-bridge): de `asyncio.run`-grens is níet de productie-seam — die draait op een actieve event-loop-thread — en de "sync wrapper in een worker thread" was de secundaire fallback, niet de productie-context. | [`token-saver-mechanismen-decision.md`](./token-saver-mechanismen-decision.md) | `c31333bf…` |
| 2026-07-25 | Heroverweging: de bridge-fractie uit de vorige 2026-07-25-rij bleek twee productie-condities te missen — een actieve event-loop-thread én een open claim+move-transactie. De sync wrapper moest dus zowel het loop-probleem als de SQLite-write-lock oplossen om de feature daadwerkelijk in productie te activeren. | ✅ **Twee fixes in één lijn: worker-thread event loop voor de async kern, en pre-transport `session.commit()` in `_run_card`.** De vorige 2026-07-25-rij claimde dat de sync wrapper in een worker-thread de `asyncio.run`-grens niet sluit; dat klopte voor de secundaire sync-caller, maar **productie doet het omgekeerde**: `_run_card` is async en roept `card_transport(...)` inline aan op zijn event-loop-thread. `asyncio.run()` in een lopende loop gooit `RuntimeError: cannot be called from a running event loop` — dus de wrapper retourneerde altijd `failed`, lekte een un-awaited-coroutine warning, en de activity-feed note is nooit gepost. **Fix 1 (loop):** `_install_rtk_for_dispatch` detecteert nu een lopende loop en voert de async kern uit op één private worker-thread met eigen event loop (zo niet, blijft `asyncio.run()` op de huidige thread — secundaire caller). De coroutine wordt pas ín die execution context gemaakt, dus een bridge-fout lekt geen un-awaited coroutine. **Fix 2 (DB-lock):** `_run_card` commit de claim+move vóór de verse worktree-transport-call. Alleen dan verplaatsen is onvoldoende: de SQLite-write-lock die `_run_card` op de kanban-DB hield blokkeerde de worker-thread-DB-connection van `post_note` (aiosqlite + één schrijver tegelijk). De bestaande exception-tak compenseert nu nog steeds release/move na een spawn-fout; de pre-commit verandert daar niets aan. **Fix 3 (zichtbaarheid):** `_post_rtk_bridge_failure_async` post best-effort `**Note:** Token saver fail-open: bridge <ExceptionType>: <reden>` wanneer de bridge zelf crasht vóór de async kern een note kon schrijven. De activity-feed note-teksten zijn nu **exact** `**Note:** Token saver activated: RTK 0.43.0` en `**Note:** Token saver fail-open: <reden>` — geen dubbele `Token saver:` prefix meer (de aanroepers geven nu de volledige canonieke string door aan `post_note`, dat prepended alleen `": "` als er nog geen prefix-staat). **Drie nieuwe regressietests** in `test_token_saver.py`: `test_sync_bridge_works_when_called_inside_running_event_loop` (pytest-asyncio-loop; verwacht `active` + exacte note), `test_dispatch_commits_claim_before_running_worktree_token_saver` (rijdt via `dispatch_card` met een worktree-transport; bewijst dat de worker-DB niet blokkeert op de dispatch-transactie), en `test_sync_bridge_failure_posts_fail_open_note` (monkeypatcht de async kern om te crashen; verwacht `failed` + bridge-fail-open-note, geen exception die de spawn breekt). De bestaande `test_sync_bridge_posts_activated_note_on_real_dispatch` is hernoemd naar `test_sync_bridge_posts_activated_note_from_non_async_caller` met een docstring die expliciet zegt dat dit de secundaire sync-caller dekt, niet productie. Tests: 23/23 unit + 4/4 kolom-migratie + 4/4 API groen. **Lockstep-meting** met `scripts/measure-token-saver.sh compare` (RTK 0.43.0, deze feature-worktree, N=2 trials): `pass_tests=1` in alle zes de runs → **geen kwaliteitsregressie**. `real-saver` `cache_read` is op N=2 niet eenduidig (trial 1: 930k vs baseline 296k, **3×**; trial 2: 386k vs 779k, **0,49×**) — de variantie is orde-grootte groter dan het proxy-effect, en de netto-opbrengst-eis is op deze N niet hard claim-baar voor `real-saver`. De proxy (`with-saver`) reproduceert §8's verwachting in beide trials (trial 2: input −73%, cache_read −54%, output −35%, `pass_diff=1`). **Geen promotion naar default-on**: de kaart-eis was opt-in met `default off`, en de `real-saver`-meting levert op deze N geen grond om die voorzichtigheid los te laten. Een hogere-N vervolgmeting hoort in een eigen kaart, niet in deze. Volledige tabellen + scenario-uitsplitsing in `token-saver-mechanismen-decision.md` §8 onder "Gemeten effect". **Heropenen** bij: (e) een race tussen twee gelijktijdige dispatches op dezelfde card waardoor de HLC-tick van `_clock_for` de dedup niet strak genoeg serialiseert (dubbele notes), (f) de pre-spawn-commit veroorzaakt aantoonbaar claim/reaper-gedrag dat de bestaande compensatiepad-tests niet afvangen, of (g) een N≥6 vervolgmeting op `real-saver` die de cache_read-variantie reduceert tot onder de proxy-effect-grens. | [`token-saver-mechanismen-decision.md`](./token-saver-mechanismen-decision.md) | `c31333bf…` |
| 2026-07-25 | De activity-feed-claim uit de 2026-07-24-rij was loos: `post_note` bestond als helper en was unit-getest, maar de dispatch-bridge riep 'm niet aan — een reviewer flagde dat en de kaart ging weer open. Hoe herstellen we het zonder de 2026-07-24-fix te herzien? | ✅ **Bridge-refactor + vier tests, geen surface-wijziging.** De sync-bridge `_install_rtk_for_dispatch` is gesplitst in een dunne `asyncio.run`-wrapper en een async kern `_install_rtk_for_dispatch_async` die dezelfde private kanban-engine opent, `maybe_install` aanroept, en voor de **active** + **failed (fail-open)** paden in dezelfde sessie `post_note` aanroept met de juiste tekst (`activated: <reason>` of `fail-open: <reason>`) en commit. De **inactive**-pad (per-lane off, board-wide off) post niets — die loopt op élke Backlog-dispatch en zou de activity-feed overstromen. De bridge geeft nu `(status, reason)` terug in plaats van alleen `status`, zodat de `RTK_TELEMETRY=off`-beslissing en de activity-feed-tekst aan dezelfde bron gekoppeld zijn — geen tweede afleiding in de caller. **Vier tests in `test_token_saver.py`** — drie voor de async-kern (`test_dispatch_bridge_posts_activated_note_on_active`, `test_dispatch_bridge_posts_fail_open_note_on_missing_binary`, `test_dispatch_bridge_silent_on_inactive_path`) die monkeypatchen `settings.kanban_database_url` naar de test-DB en de **async kern** direct aanroepen (omdat `asyncio.run` vanuit een pytest-asyncio test niet mag), plus één sync-bridge test (`test_sync_bridge_posts_activated_note_on_real_dispatch`) die de **sync wrapper in een worker thread** draait en daarmee bewijst dat de `asyncio.run`-grens de note niet sluit — precies de test die een FCR-ronder expliciet eiste. Geen surface-wijziging in `token_saver.py`, geen frontend-touch, geen meet-harnas-wijziging. Tests: 20/20 unit + 4/4 kolom-migratie + 4/4 API groen. **Heropenen** bij: een race tussen twee gelijktijdige dispatches op dezelfde card waardoor de HLC-tick van `_clock_for` de dedup niet strak genoeg serialiseert (dubbele notes), of een operator wil de note-tekst in de UI op de card zelf zien (nu alleen in de activity-feed-component). ↩︎ herzien door de rij direct hieronder (2026-07-25, productie-bridge): de `asyncio.run`-grens is níet de productie-seam — die draait op een actieve event-loop-thread — en de "sync wrapper in een worker thread" was de secundaire fallback, niet de productie-context. | [`token-saver-mechanismen-decision.md` §8-bis](./token-saver-mechanismen-decision.md) | `c31333bf…` |
| 2026-07-25 | Heroverweging: de bridge-fractie uit de vorige 2026-07-25-rij bleek twee productie-condities te missen — een actieve event-loop-thread én een open claim+move-transactie. De sync wrapper moest dus zowel het loop-probleem als de SQLite-write-lock oplossen om de feature daadwerkelijk in productie te activeren. | ✅ **Twee fixes in één lijn: worker-thread event loop voor de async kern, en pre-transport `session.commit()` in `_run_card`.** De vorige 2026-07-25-rij claimde dat de sync wrapper in een worker-thread de `asyncio.run`-grens niet sluit; dat klopte voor de secundaire sync-caller, maar **productie doet het omgekeerde**: `_run_card` is async en roept `card_transport(...)` inline aan op zijn event-loop-thread. `asyncio.run()` in een lopende loop gooit `RuntimeError: cannot be called from a running event loop` — dus de wrapper retourneerde altijd `failed`, lekte een un-awaited-coroutine warning, en de activity-feed note is nooit gepost. **Fix 1 (loop):** `_install_rtk_for_dispatch` detecteert nu een lopende loop en voert de async kern uit op één private worker-thread met eigen event loop (zo niet, blijft `asyncio.run()` op de huidige thread — secundaire caller). De coroutine wordt pas ín die execution context gemaakt, dus een bridge-fout lekt geen un-awaited coroutine. **Fix 2 (DB-lock):** `_run_card` commit de claim+move vóór de verse worktree-transport-call. Alleen dan verplaatsen is onvoldoende: de SQLite-write-lock die `_run_card` op de kanban-DB hield blokkeerde de worker-thread-DB-connection van `post_note` (aiosqlite + één schrijver tegelijk). De bestaande exception-tak compenseert nu nog steeds release/move na een spawn-fout; de pre-commit verandert daar niets aan. **Fix 3 (zichtbaarheid):** `_post_rtk_bridge_failure_async` post best-effort `**Note:** Token saver fail-open: bridge <ExceptionType>: <reden>` wanneer de bridge zelf crasht vóór de async kern een note kon schrijven. De activity-feed note-teksten zijn nu **exact** `**Note:** Token saver activated: RTK 0.43.0` en `**Note:** Token saver fail-open: <reden>` — geen dubbele `Token saver:` prefix meer (de aanroepers geven nu de volledige canonieke string door aan `post_note`, dat prepended alleen `": "` als er nog geen prefix-staat). **Drie nieuwe regressietests** in `test_token_saver.py`: `test_sync_bridge_works_when_called_inside_running_event_loop` (pytest-asyncio-loop; verwacht `active` + exacte note), `test_dispatch_commits_claim_before_running_worktree_token_saver` (rijdt via `dispatch_card` met een worktree-transport; bewijst dat de worker-DB niet blokkeert op de dispatch-transactie), en `test_sync_bridge_failure_posts_fail_open_note` (monkeypatcht de async kern om te crashen; verwacht `failed` + bridge-fail-open-note, geen exception die de spawn breekt). De bestaande `test_sync_bridge_posts_activated_note_on_real_dispatch` is hernoemd naar `test_sync_bridge_posts_activated_note_from_non_async_caller` met een docstring die expliciet zegt dat dit de secundaire sync-caller dekt, niet productie. Tests: 23/23 unit + 4/4 kolom-migratie + 4/4 API groen. **Lockstep-meting** met `scripts/measure-token-saver.sh compare` (RTK 0.43.0, deze feature-worktree, N=2 trials): `pass_tests=1` in alle zes de runs → **geen kwaliteitsregressie**. `real-saver` `cache_read` is op N=2 niet eenduidig (trial 1: 930k vs baseline 296k, **3×**; trial 2: 386k vs 779k, **0,49×**) — de variantie is orde-grootte groter dan het proxy-effect, en de netto-opbrengst-eis is op deze N niet hard claim-baar voor `real-saver`. De proxy (`with-saver`) reproduceert §8's verwachting in beide trials (trial 2: input −73%, cache_read −54%, output −35%, `pass_diff=1`). **Geen promotion naar default-on**: de kaart-eis was opt-in met `default off`, en de `real-saver`-meting levert op deze N geen grond om die voorzichtigheid los te laten. Een hogere-N vervolgmeting hoort in een eigen kaart, niet in deze. Volledige tabellen + scenario-uitsplitsing in `token-saver-mechanismen-decision.md` §8 onder "Gemeten effect". **Heropenen** bij: (e) een race tussen twee gelijktijdige dispatches op dezelfde card waardoor de HLC-tick van `_clock_for` de dedup niet strak genoeg serialiseert (dubbele notes), (f) de pre-spawn-commit veroorzaakt aantoonbaar claim/reaper-gedrag dat de bestaande compensatiepad-tests niet afvangen, of (g) een N≥6 vervolgmeting op `real-saver` die de cache_read-variantie reduceert tot onder de proxy-effect-grens. | [`token-saver-mechanismen-decision.md` §8 + §8-bis](./token-saver-mechanismen-decision.md) | `c31333bf…` |
| 2026-07-22 | Product-analyses van één externe applicatie ("hier is een URL — wat kunnen we hiervan leren of overnemen?") gebeuren ad hoc. Uniformeren via een sjabloon, een eigen agent/persona, of een skill? | **Een skill (`product-analysis`), geen aparte agent — en het sjabloon leeft ín die skill.** De vier ad-hoc product-analyses tot nu toe (`openhands-analyse.md` 2026-07-13, `jira-lessen-analyse.md` 2026-07-14, `9router-integratie-analyse.md` 2026-07-19, `lemma-platform-analyse.md` 2026-07-21) zijn allemaal door de **bestaande** analyst-persona in modus 2 geproduceerd en convergeerden vanzelf al op dezelfde 8–11 H2-structuur — wat ontbrak was niet een rol maar een **procedure**. Wat tussen die runs wél dreef, waren precies de dingen die een sjabloon niet vastlegt: `openhands-analyse.md` §7 zette de vervolgtaken als prozalijst neer ("niet in deze kaart aangemaakt") terwijl `lemma-platform-analyse.md` §7 ze als echte kind-kaarten aanmaakte — exact de faalklasse die `analyse-orphaned-followups-audit.md` al documenteerde als "analyses die tot niets geleid hebben". Een eigen persona is mechanisch goedkoop (`_persona_for_card`, `dispatch.py:1187-1198`, leest `.claude/agents/<card.agent>.md` — nul backend-wijziging) maar **onbereikbaar zonder handwerk**: `WORK_TYPES` ligt vast op `analysis` / `feature` / `bug` / `chore` (`schemas.py:35`), dus een product-analyse-kaart blijft op `work_type` = `analysis` en zou per kaart een handmatig `agent`-veld vereisen, terwijl een skill zichzelf herkent aan de kaarttekst die de gebruiker tóch al schrijft ("Product analyse - <url>"). Bovendien zou een tweede persona **189 van de 383 regels** van `analyst.md` dupliceren die niets met product-analyse te maken hebben (outcome-contract, product-taal, worktree-scope, projectconventies) en die in sync moeten blijven met de Done-poort in `mcp_server.py`. Geleverd: de skill met acht-stappen-procedure + doc-sjabloon, plus een verwijzing in `analyst.md` modus 2. Bewust niet: nieuwe `work_type`, los sjabloonbestand in `docs/cockpit/`, kaart-detectie in de dispatcher. **Heropenen** bij: gedrag dat écht rolniveau wordt (eigen ship-workflow/model/outcome-contract), een routeringssignaal fijner dan `work_type`, of twee analyses die de skill negeren terwijl hij beschikbaar is. | [`product-analyse-methode-decision.md`](./product-analyse-methode-decision.md) | `8394f725…` |
| 2026-07-21 | Telt `cache_read` mee in het Claude-abonnementsquotum? — beslecht met een gecontroleerde meting i.p.v. webresearch (de boekhoudvraag die het hele token-saver-spoor kantelt). | ✅ **Scenario B: `cache_read` telt NIET mee** — gemeten, niet geïnfereerd. Gecontroleerde injectie-meting op dit abonnement tegen Anthropic's eigen server-side teller (`five_hour.utilization` uit `/api/oauth/usage`, de bron achter `/usage`): effectief cache_read-gewicht **w = 0,014** per token t.o.v. een ongecachte input-token — statistisch nul, SSE loopt monotoon op naar het "telt vol mee"-model (w=1, SSE 89,98 vs. w=0, SSE 8,97). **Model-vrij kernbewijs:** twee intervallen bewogen allebei **exact 4 procentpunt** terwijl hun cache_read 1,8× verschilde (2,05 M vs. 3,69 M) bij vrijwel gelijk ongecacht volume — geen regressie nodig. Cockpit's eigen `subscription_pool`-signaal kón dit **niet** beslechten: `AnthropicUsageProvider` telt zelf `cache_read` bij het totaal, dus dat signaal is circulair (→ aparte follow-up: het overschat verbruik ~96% en pauzeert abonnementen te vroeg). **Gevolg voor het token-saver-spoor:** Scenario B uit `token-saver-mechanismen-decision.md` §2.3 is werkelijkheid → RTK ≈ **1,6%** / Caveman ≈ **6,7%**; **Caveman (output-reductie) is het sterkste mechanisme**, niet RTK. Dit **bevestigt en upgradet van inferentie naar meting** de regel bij `token-saver-meet-harnas.md` (`6b67df66…`) en **corrigeert** de "beide scenario's onbeslist / #24147 leunt naar A"-framing bij `3abcd501…`: [claude-code#24147](https://github.com/anthropics/claude-code/issues/24147) beschrijft token-*boekhouding* die cache_read meesommeert, niet werkelijke quotum-consumptie. Prijstabel geverifieerd tot 7 cijfers uit gerapporteerde `costUSD` (cache_read = 0,1× input-prijs, geen quotum-gewicht). Reproductie: `scripts/measure-cache-read-quota.sh {verify-pricing,sample,amplify,fit}`. **Heropenen** bij: Max-account meet het tegendeel, of Anthropic wijzigt de cache-boekhouding. | [`cache-read-quota-decision.md`](./cache-read-quota-decision.md) | `97e623f9…` |
| 2026-07-21 | De productnaam "Claude Cockpit" noemt een vendor terwijl de kern juist vendor-agnostisch is — hoe rebranden we naam, logo en thema? | **GO — nieuwe naam "Agent Cockpit", nieuw indigo-primair thema, nieuw vendor-neutraal logo.** De productnaam "Claude Cockpit" claimt een vendor die het product juist níet wil claimen: de orchestratie-kern (dispatch, worktrees, agent mail, dependency-DAG) is bewust agent- en vendor-onafhankelijk, en er draaien vandaag al vier providers. "Agent Cockpit" haalt de vendor uit de naam en **behoudt het interne identifier `cockpit`**, dat load-bearing is in `docs/cockpit/`, `scripts/cockpit.sh`, `scripts/cockpit-doctor.sh` en de `cockpit-kanban` MCP-server — de rename krimpt daarmee tot "laat het `claude`-voorvoegsel vallen" in plaats van een volledige identifier-sweep. Het thema verruilt Anthropic-oranje (`15 63%`, letterlijk `#D97757` in het logo) voor een eigen indigo (`258 62% 48%` licht / `258 70% 68%` donker) dat in lichte modus **beter** contrasteert dan het huidige oranje (7,86:1 vs 4,76:1) en in donkere modus ruim boven AA blijft (5,43:1). 4 vervolgkaarten. Bewust **buiten scope**: de twee `localStorage`-sleutels, `~/.claude-registry/` en `claude_registry.db` — die dragen bestaande gebruikersstaat, en hernoemen kost een migratie of een stille reset voor nul zichtbaar effect. | [`rebrand-decision.md`](./rebrand-decision.md) | `ce0ea8d6…` |
| 2026-07-28 | De 2026-07-21-rebrand mat de string-sweep maar liet de **repo-naam** zelf onbesproken. Hernoemen we de repo naar `agent-cockpit`, en zo ja: forge, lokale remote en checkout-directory in één keer? | **Gefaseerd — forge nu, sleutelmigratie apart, directory niet.** De board-identiteit is afgeleid, niet opgeslagen: `resolve_project_key()` ([`project_key.py:41`](../../backend/app/kanban/project_key.py)) shelt bij elke request uit naar `git remote get-url origin`. Forge-rename **plus** remote-update verschuift de sleutel `git:github.com/guillaumevandevelde/claude-cockpit` → `…/agent-cockpit` en verweest daarmee **13.372 rijen** over zes kolommen in beide stores (`kanban_ops`/`kanban_cards`/`kanban_columns`/`kanban_gates`.`project_key`, `kanban_meta`.`key`, `security_audit`.`project_key`). Er gaat niets stuk met een foutmelding — het bord leest als **leeg** en autodispatch/max-sessions/shipmode/gates zijn weg. Doorslaggevend voor de fasering is dat GitHub na een rename een **redirect** achterlaat: zolang `origin` de oude URL houdt, blijven `fetch`/`push` werken en blijft de sleutel stabiel, dus de forge-rename kost lokaal niets. **Fase 1 ✅ 2026-07-28**: `gh repo rename agent-cockpit -R <owner>/<oud>` — de `-R`-vorm laat de lokale remote expliciet ongemoeid (de vorm zónder `-R` schrijft 'm wél bij en zou fase 2 ongepland afdwingen); geverifieerd dat remote, `resolve_project_key()`, de draaiende stack en drie live sessies onaangeroerd bleven. **Fase 2 ⏳** via `scripts/migrate-project-key.py`: dry-run by default, snapshot van beide DB's naar `~/.claude-registry/backups/` via de `sqlite3`-backup-API (een platte kopie is niet consistent terwijl de backend een connectie openhoudt), `--update-remote` zet remote en bord in dezelfde run om zodat ze niet uit elkaar lopen, en een **claim-guard** weigert te draaien zolang een niet-Done kaart een `claimed_by` heeft — onder een lopende dispatcher hernoemen trekt de kaart weg onder een sessie die 'm nog vasthoudt. End-to-end getest tegen kopieën van beide productie-DB's: nul oude-sleutel-rijen, exacte telling-pariteit, en de acht prefix-embedded `kanban_meta`-sleutels behielden hun namespace + card-id-suffix. **Fase 3 (checkout-directory) ❌ niet gepland**: 24 geregistreerde worktrees, ~70 hardgecodeerde absolute paden en alle path-afgeleide DB-kolommen; een remote-rename raakt die niet. **Heropenen** bij: een tweede checkout van dit repo op dezelfde machine (dan wordt de path-as een echte tweede dimensie), of als fase 2 langer dan een release blijft liggen en de forge/lokale naamdiscrepantie zelf verwarring gaat kosten. | [`rebrand-decision.md` §2.3](./rebrand-decision.md) | `ce0ea8d6…` |
| 2026-07-21 | Is auto-dispatch hard gekoppeld aan het Anthropic-abonnement, en moet "dispatch" naar een goedkoop API-model — of kan het deterministisch? | **Premisse deels onjuist; GO op één smal gat.** "Dispatch" dekt drie lagen. Het **dispatch-besluit is al 100% deterministisch**: geen LLM-SDK in `backend/requirements.txt`, nul uitgaande model-calls in `backend/app` — er valt niets naar een goedkoop model te verplaatsen, en dat toevoegen zou determinisme juist wegnemen. De **spawn-env-laag is al vendor-onafhankelijk** (4 providers; MiniMax draait vandaag API-key-gebaseerd via dezelfde `claude`-CLI). Anthropic is dus een **default** (`or PROVIDER_ANTHROPIC`, `dispatch.py:1140/3242/3480`), geen slot. Het echte gat: **`anthropic-compatible` (vrije `base_url`+API-key) is alleen bereikbaar vanuit de interactieve New-Session-dialog, niet vanuit auto-dispatch** — het worktree-transport bouwt `SpawnCommandOptions` zonder endpoint-velden (`dispatch.py:2419-2423`), terwijl de resolutie alleen in `api/v1/runs/router.py:497` leeft. Plus: de pool **accepteert** `anthropic-compatible` (`subscription_pool.py:73`) → `ValueError` bij spawn → 3 failures → Impediment; `column.default_provider` is ongevalideerd; headless-transport mist minimax-key én endpoint-velden (`headless_runner.py:303`). Uitkomst: 4 vervolgkaarten, **uitsluitend API-key-tier**, endpoint-naam op de bestaande JSON-carriers (geen migratie), default blijft `anthropic`. Heropent **niet** `a410468d` (proxy-op-subscription-auth), `27cdc2bd` (9router) of `290f6fb7` (same-vendor OAuth-multi-account) — zie §4.1. Besparingsclaim expliciet **ongemeten**; rechtvaardiging is optionaliteit, niet prijs. | [`dispatch-vendor-koppeling-analyse.md`](./dispatch-vendor-koppeling-analyse.md) | `03734e90…` |
| 2026-07-21 | Welk token-saving-mechanisme integreren we — RTK, Headroom, Caveman of Ponytail — nu de product-owner besloten heeft te bouwen? (los van de router-vraag) | ✅ **GO op RTK** (conditioneel) · ⛔ **NO-GO op Headroom** · ⚠️ **conditionele GO op Caveman** (opt-in, output-onkritische lanes) · ⛔ **NO-GO op Ponytail**. **De kaart-premisse is gecorrigeerd:** RTK comprimeert géén `tool_result`-payloads — het is een `PreToolUse`-**hook** die het *Bash-commando* herschrijft vóór uitvoering (geverifieerd: `git status` → `rtk git status`; onbekend commando → lege output = fail-open). Het raakt de API-request nooit, dus het is **cache-veilig by construction** en heeft geen ToS-oppervlak; `9router-integratie-analyse.md` §4.2's "prompt-mutatie op de hot-path" gold voor RTK dus niet. Headroom valt af op integratievorm, niet op kwaliteit: zijn enige werkende Claude-Code-vorm zet `ANTHROPIC_BASE_URL` naar een lokale proxy (bevestigd in de package-source) = de subscription-OAuth-MITM die **2026-07-15 (`a410468d…`) en 2026-07-19 al is afgewezen**; bovendien gate't die base-URL aantoonbaar drie CC-capabilities (#746 schema-deferral — ironisch, dat kóst tokens; #1158 1M-context; #1779 Remote Control, onherstelbaar vanaf 2.1.196, wij draaien 2.1.216). De library-vorm heeft geen aangrijpingspunt: **Cockpit doet per ontwerp nul LLM-calls**. **Gemeten** (25 echte dispatch-sessies, 2.001 turns, 190M tokens; Anthropic's eigen tokenizer via `claude -p --output-format json`): RTK haalt 52–98% op gedekte commando's (`git diff` −52,1%, `grep -r` −76,2%, `ls` −64,2%), maar raakt maar **41,4%** van het tool-outputvolume (`Read` = 45,7% ligt buiten de Bash-hook) en bespaart **0%** op `rtk read`/`git log` (default `--level none`). **Kernonzekerheid, eerlijk gelabeld:** telt `cache_read` (95,9% van al het verbruik) mee in het abonnementsquotum? Officieel voor de **API**: nee. Voor **Claude Code op abonnement**: niet gedocumenteerd; sterkste evidentie is [claude-code#24147](https://github.com/anthropics/claude-code/issues/24147) (*"cache reads = 99,93% of usage quota"*, 30 dagen data, ratio 1.310:1) mét tegenspraak in dezelfde thread. Daarom **beide scenario's doorgerekend** — en ze zijn **anti-gecorreleerd**: telt het mee → RTK **7,4%** / Caveman 0,26%; telt het niet mee → RTK **1,6%** / Caveman **6,7%** (output is dan 10,3% van de noemer). Dus geen enkele keuze, maar een **hedge**: beide opt-in en per-lane. Dit **spreekt de 2026-07-21-regel bij `token-saver-meet-harnas.md` tegen** (die inferereerde "telt waarschijnlijk niet mee", zelf gelabeld als niet-autoritatief) — de verantwoorde uitkomst is die knoop niet op aanname doorhakken maar meten. **Voorwaarden bij de RTK-GO:** per-lane via worktree-lokale `.claude/settings.json` (nooit `rtk init -g` — dat muteert `~/.claude/` van élke concurrente sessie), `grep` carve-out (RTK omzeilt de ugrep-shim op deze host → "Is a directory" i.p.v. 868 matches), niet op ship-lanes (`git diff` truncatie dropt **505 van 937** gewijzigde regels — wél gemarkeerd met `(N lines truncated)`, dus niet "stil" zoals §4.2 stelde), versie pinnen (0.43.0, repo 6 mnd oud), telemetrie uit (is al default). **Heropenen** bij: RTK krijgt verliesvrije modus mét winst, Headroom krijgt een vorm zonder `ANTHROPIC_BASE_URL`, Anthropic documenteert `cache_read` vs. abonnement, of de tool-mix kantelt (`Read` onder `Bash`). **BESLECHT 2026-07-21 (`97e623f9…`, zie de regel bovenaan het register):** de anti-gecorreleerde boekhoudvraag is nu **gemeten** — `cache_read` telt niet mee (Scenario B), dus **Caveman ≈ 6,7% > RTK ≈ 1,6%**; de hedge blijft staan, maar de bouwinspanning weegt richting output-reductie. | [`token-saver-mechanismen-decision.md`](./token-saver-mechanismen-decision.md) | `3abcd501…` |
| 2026-07-21 | Kunnen we token-savers (RTK/Caveman/Ponytail) op een echte Cockpit-dispatch-workload **meten** (drie verbruiks-componenten + kwaliteitsregressie) — voordat we ze als default kunnen zetten? — K1 van `9router-integratie-analyse.md` §9. | **Shell-only proxy + golden task + scratch worktree.** Eén `compare`-run, één 3-rij Markdown-tabel met de drie verbruiks-componenten **apart** (`input_tokens` / `cache_creation_input_tokens` / `cache_read_input_tokens`, NOOIT samengeteld — kaart-eis) + twee binaire kwaliteits-signalen (`pass_tests` + `pass_diff`). Proxy is byte-stabiel (SHA-256-invariant) en emuleert 3 laagjes (Caveman-prelude, Ponytail-tail, RTK-achtige line-dedup + blank-collapse) zonder LLM-pass of HTTP-rewrite — dus **lower bound**: breekt deze naïeve mutatie kwaliteit, doet een echte saver dat ook (waarschijnlijk vaker). Golden task = 1-line revert van `b30a9bb` op `dispatch.py:2244`; bijbehorende regression tests staan al op master. **Eerste meting:** `cache_read` daalt **−42,4%** (1,15 M → 0,66 M); `input_tokens` −0,5% (gemaskeerd door prefix-cache); `output_tokens` −14,3%; `pass_diff=1` (agent paste juiste fix toe); `pass_tests=0` is **scoring-infra-failure** (worktree-venv ontbreekt, geen saver-regressie). **`cache_read` en abonnementslimieten:** telt **niet** mee voor API-ITPM ([platform.claude.com/docs/en/api/rate-limits](https://platform.claude.com/docs/en/api/rate-limits)) en wordt gefactureerd op 10% van input-prijs. claude.ai-subscriptions ([support.anthropic.com/en/articles/11647753](https://support.anthropic.com/en/articles/11647753)) documenteren caching **niet** — inferentie: quotum is rolling 5h message-window, dus `cache_read` trekt zeer waarschijnlijk niets af; **expliciet "best available inference, not authoritative"**. **Heropenen** bij: 9router écht geïnstalleerd (vervang proxy door echte RTK + herhaal), K3 multi-task + N-runs + variantie + worktree-venv-fix, of Anthropic documenteert `cache_read` vs subscriptions expliciet. **BEVESTIGD 2026-07-21 (`97e623f9…`, zie de regel bovenaan het register):** deze inferentie ("cache_read trekt zeer waarschijnlijk niets af", zelf gelabeld als niet-autoritatief) is nu **gemeten bevestigd** — geüpgraded van inferentie naar meting (Scenario B). | [`token-saver-meet-harnas.md`](./token-saver-meet-harnas.md) | `6b67df66…` |
| 2026-07-21 | Kunnen we token-savers (RTK/Caveman/Ponytail) op een echte Cockpit-dispatch-workload **meten** (drie verbruiks-componenten + kwaliteitsregressie) — voordat we ze als default kunnen zetten? — K1 van `9router-integratie-analyse.md` §9. | **Shell-only proxy + golden task + verse scratch worktree per run + counterbalanced trials.** Eén `compare`-run = 2 trials × 2 varianten (baseline/saver) in **omgekeerde volgorde** (trial 1 = baseline→saver, trial 2 = saver→baseline), zodat noch gedeelde worktree-state noch run-volgorde een confounder is. Per run: verse scratch-worktree vanaf `HEAD`, golden-task-revert opnieuw toegepast, agent invoked, `score_golden` op de geïsoleerde werkende boom. Resultaat-tabel met de drie verbruiks-componenten **apart** (`input_tokens` / `cache_creation_input_tokens` / `cache_read_input_tokens`, NOOIT samengeteld — kaart-eis) + twee binaire kwaliteits-signalen (`pass_tests` + `pass_diff`). Proxy byte-stabiel (SHA-256-invariant), emuleert 3 laagjes (Caveman-prelude, Ponytail-tail, RTK-achtige line-dedup + blank-collapse) zonder LLM-pass of HTTP-rewrite — dus **lower bound**. Golden task = 1-line revert van `b30a9bb` op `dispatch.py:2244`; bijbehorende regression tests staan al op master. **Eerste counterbalanced meting (2026-07-21, 4 runs):** `input_tokens` daalt in **beide** trials (gem. −36.5K, ~45%); `output_tokens` daalt in **beide** trials (gem. −2.7K, ~72%, Caveman-prelude werkt); `cache_read_input_tokens` is **volgorde-gedomineerd** (trial 1 +39K, trial 2 −236K) — Anthropic's sessie-warme prompt-cache domineert het eventuele saver-effect op N=2. **`cache_read`-richting is onbeslist op deze N**; de eerdere −42,4%-headline is **ongeldig** wegens order-confound (gold thread 2026-07-21, deze run). **`cache_read` en abonnementslimieten:** telt **niet** mee voor API-ITPM ([platform.claude.com/docs/en/api/rate-limits](https://platform.claude.com/docs/en/api/rate-limits)) en wordt gefactureerd op 10% van input-prijs. claude.ai-subscriptions ([support.anthropic.com/en/articles/11647753](https://support.anthropic.com/en/articles/11647753)) documenteren caching **niet** — inferentie: quotum is rolling 5h message-window, dus `cache_read` trekt zeer waarschijnlijk niets af; **expliciet "best available inference, not authoritative"**. `pass_tests=0` in alle runs is **scoring-infra-failure** (worktree-venv ontbreekt), geen saver-regressie. Raw artifacts in [`measure-evidence/2026-07-21-counterbalanced/`](./measure-evidence/2026-07-21-counterbalanced/). **Heropenen** bij: 9router écht geïnstalleerd (vervang proxy door echte RTK + herhaal), K3 multi-task + cold-prefix + N-runs + variantie + worktree-venv-fix + alternatieve-geldige-oplossing-detectie, of Anthropic documenteert `cache_read` vs subscriptions expliciet. | [`token-saver-meet-harnas.md`](./token-saver-meet-harnas.md) | `6b67df66…` |
| 2026-07-19 | [9Router](https://github.com/decolua/9router) integreren als geheel, ernaast draaien als provider-router, of niet — en vervangt het onze provider-laag? | **NO-GO op "als geheel"; conditionele GO op "ernaast", smal en opt-in.** De premisse "9router lijkt matuurder" is een categoriefout: 9router is een **inference-router** (per *request*), Cockpit's provider-laag een **spawn-configurator** (per *sessie*, vóór het proces start) — ze zitten op verschillende lagen. Gemeten is 9router **breder** (40+ providers), niet matuurder: repo 6,5 maand oud (2026-01-05), `v0.5.35`, **519 open PR's**, 700 open vs. 592 gesloten issues, JS zonder types. Vendoring afgewezen (12 MB Next.js naast getypeerd Python/FastAPI; Cockpit doet per ontwerp **nul** LLM-calls; 519-PR-onderhoudslast; en alles wat het biedt loopt tóch via HTTP). Bestaande provider-functionaliteit **blijft ongewijzigd** — "provider" betekent hier drie dingen en 9router raakt alleen `provider_env.py` (~260 regels); de CLI-registry `agentic_cli/` (~1.400 regels) staat er volledig los van. Echte opbrengst = **format-translatie** (geverifieerd: echte Anthropic-native `/v1/messages` + `count_tokens`, met `next.config.mjs`-rewrite `/v1/v1/*`→`/api/v1/*` zodat `ANTHROPIC_BASE_URL` werkt) + **mid-sessie-failover** (vandaag sterft een sessie op een limiet, blijft de `agent:`-claim hangen en verliest de re-dispatch werk). Prijs: de "unlimited free"-tiers (Kiro, subscription-OAuth) draaien op andermans product-OAuth en raken exact het Anthropic-account waar álle dispatch op draait — **de proxy-op-subscription-auth-route is 2026-07-15 al afgewezen** (`a410468d…`), en die blijft staan. Daarom: alleen de **API-key-tier**, token-savers **uit** (RTK staat default AAN en muteert `tool_result`; Caveman/Ponytail injecteren gedragsprompts — stille kwaliteitsdegradatie in een autonome sessie), cloud-sync uit, loopback-only, nooit default. **LiteLLM is voor die overgebleven scope functioneel inwisselbaar en wint op maturiteit** → K2 kiest. De 20–40%-tokenclaim is een **ongemeten vendor-claim** → K1 meet vóór er iets op gebouwd wordt. **Heropenen** bij: 9router `1.x` + verwerkte PR-berg, legitiem gratis quotum zonder product-OAuth, Cockpit doet zelf LLM-calls, of K1 meet substantiële besparing zonder gedragsdegradatie. | [`9router-integratie-analyse.md`](./9router-integratie-analyse.md) | `27cdc2bd…` |
| 2026-07-19 | De vier Done-move-workflowpoorten (`summary_required`, analyse-outcome-contract, parent-parking, reviewer-gate) zitten alleen op `mcp_server.move_card`; de REST/UI-move omzeilt ze allemaal — centraliseren of documenteren? | **Documenteren — de asymmetrie is opzettelijk.** De poorten dwingen *agent*-discipline af (ze bestaan omdat prompt-niveau-instructies drie rondes lang genegeerd werden zolang geen machinepad ze verifieerde); een mens is niet de te disciplineren partij maar de autoriteit die corrigeert wanneer een agent een kaart verkeerd parkeerde. Zonder ongegate override-oppervlak is een kaart die door een poort verkeerd klemt (bv. parent-parking op een verweesd kind) alleen met een DB-edit los te trekken. Centraliseren is bovendien technisch uitgesloten zonder UI-herontwerp: `MoveRequest` draagt alleen `column` + `rank` — geen `summary`, geen `outcome` — dus de poorten op het REST-pad leggen zou **elke** UI-drag-naar-Done weigeren. Dat is een aparte productbeslissing, geen consistentie-fix. Vastgelegd als expliciete conventie met poorten-tabel + consequenties voor nieuwe poorten/tooling. Bevestigt en expliciteert de zijdelingse formulering in de 2026-07-18 reviewer-rij. | [`kanban-conventions.md` §3b](./kanban-conventions.md) | `19763bf4…` |
| 2026-07-18 | Onafhankelijke reviewer-agent + review-kolom-gate voor **álle** kaarten vóór Done — bouwen? (heropent de 2026-07-10-beslissing die juist voor de lichtere in-sessie FCR koos) | ✅ **GO — optie A, bouwen (mens-beslist, doorvoerkost geaccepteerd).** De 2026-07-10-FCR blijft, maar mist wat de gebruiker expliciet wil: een **onafhankelijke** gate die de engineer niet kan overslaan (de FCR is dezelfde sessie die het werk bouwde). Afgedwongen op het **agent-pad** (`mcp_server.move_card`), net als de bestaande outcome-gate/parent-parking/summary-verplichting; de dunne REST/UI-move blijft — conform diezelfde conventie — een bewust menselijk override-oppervlak. Mechanisme: `.claude/agents/reviewer.md` (creëert de `reviewer`-kolom via `sync_agent_columns`) + een redirect in `mcp_server.move_card` — een niet-reviewer-kaart die naar Done gaat wordt, mits de `reviewer`-kolom bestaat, doorgestuurd naar de reviewer-kolom met `agent` geflipt naar `reviewer` (een kolom alleen is niet genoeg — `_phase_target_agent` leest `card.agent` eerst) en de engineer-sessie opgeruimd; de dispatcher pikt 'm op als verse reviewer-sessie. Reviewer akkoord → echte Done; niet-akkoord → `report_impediment` (dat de `agent` terugzet naar de engineer voor de fix-resume). Uitgesloten: reviewer's eigen Done (anders loop) en analyse-kaarten (eigen outcome-contract + kind-kaarten zijn de review-surface). Activatie-schakelaar = kolom-bestaan (geen kolom → gedrag ongewijzigd, backwards-compat). Bekende beperking: in direct-ship-modus reviewt de reviewer ná de merge — de kaart bereikt Done pas na akkoord, maar de code staat al op master (pull-request-modus is pre-merge). | [`reviewer-agent-decision.md`](./reviewer-agent-decision.md) (REVISED 2026-07-18) | `b493d3eb…` (↩︎ herziet 2026-07-10) |
| 2026-07-18 | Code-kennisgraaf (Understand-Anything: tree-sitter + LLM → `.ua/knowledge-graph.json`) adopteren voor code-navigatie? (voorwaardelijke, uitgestelde fork-poot uit `knowledge-structure-navigation-analysis.md`) | **NO-GO nu — `not_feasible`, trigger niet gevuurd.** De kaart mocht alleen getrokken worden als code-navigatie (NIET docs) een **gemeten** pijn werd. Dat bewijs bestaat niet: geen doc en geen `Backlog`/`Impediment`-kaart registreert code-navigatie-pijn (de enige `[knowledge-structure]`-kaarten zijn de docs-poot — frontmatter + index, `25bfe803…`/`340a3010…`), en de telemetrie kan het **niet eens produceren** — `usage_service`/`dispatch_usage_service` meten tokens per sessie/model/dag zonder vraag-categorie, en "APM" is een package-manager, geen perf-monitoring. Externe evidentie leunt al tegen (83% vs 92% antwoordkwaliteit t.o.v. file-exploration). **Heropenen** alleen als per-turn/tool-call-telemetrie óf ≥3 reële sessies aantonen dat structurele code-vragen (call-graphs/impact-analyse) aantoonbaar te veel tool-calls/tokens kosten. | [`code-knowledge-graph-navigation-decision.md`](./code-knowledge-graph-navigation-decision.md) | `a4318941…` |
| 2026-07-15 | Inzicht in verbruik per subscription — zelf inkantelen of Langfuse koppelen? | **Inkantelen. Langfuse NO-GO** (conditioneel bewaard). Langfuse meet $-kosten per LLM-call; een vast-tarief-abonnement verbruikt **quota**, geen $ — het kan de gestelde vraag ("hoeveel van mijn 5h-venster is op?") structureel niet beantwoorden. Bovendien doet Cockpit zelf nul LLM-calls (het spawnt CLI's), dus er is geen instrumentatiepunt; de omwegen zijn proxy (auth-/ToS-risico op subscription-auth), OTel (metrics ≠ traces) of batch-ETL (data rondpompen die we al lokaal hebben). De data ligt er al: `UsageService` parst de JSONL al mét `model_breakdowns` — wat ontbreekt is één dimensie (`model → subscription`) + één scherm. Heropenen alleen als een §5.3-trigger vuurt (Cockpit doet eigen in-proces LLM-calls / echte pay-per-token-attributie / prompt-versiebeheer+evals). **En passant: gekwantificeerde bug** — `AnthropicUsageProvider` telt MiniMax-tokens mee (36,9% van alle tokens op deze host), dus de attributie-fix moet vóór de registry-fix landen. | [`subscription-verbruik-inzicht-analyse.md`](./subscription-verbruik-inzicht-analyse.md) | `a410468d…` |
| 2026-07-15 | Filteren we de 19 `cockpit-kanban`-MCP-tools per persona (engineer/analyst) om system-prompt-tokens te besparen? (`token-optimization-analysis.md` §4 R3) | **NO-GO — geen vervolgkaart.** De premisse ("alle 19 schemas in élke system-prompt") is achterhaald: Claude Code 2.1.210 defert MCP-schemas achter `ToolSearch`, waardoor de 19 tools **388 tokens** kosten i.p.v. ~4.994 — 1,1% van een 36.660-token baseline; de CLI vangt al ~92%. Het voorgestelde mechanisme bestaat bovendien niet: `--allowedTools`/`--disallowedTools` zijn permissie-poorten, geen schema-filters (`--allowedTools` kost netto **+109** tokens). Alleen een rol-gescopete MCP-mount zou grip geven, voor max ~184 tokens (0,5%) — tegenover permanente plumbing en een faalmodus die de leaf-spike-analyst (die de unie van beide rolsets nodig heeft) op de ship-stap breekt. Ook de matrix zelf is minder scheidbaar dan aangenomen: de **verplichte** `session-retro` roept `create_card` aan. Misfire-preventie hoort server-side (bestaand `{"error": …}`-patroon), niet in een allowlist. **Heropenen** alleen als de meting in §7 van 388 → ~5.000 springt. | [`per-persona-mcp-allowlist-decision.md`](./per-persona-mcp-allowlist-decision.md) | `28e1558e…` |
| 2026-07-15 | Hoe modelleren we "kaart wacht op een niet-kanban business-trigger" (bv. "activeert pas bij tweede-executor-provider-onboarding")? | **`metadata.gated_on` als machine-leesbare gate.** Geen dedicated kolom (disproportionele infra voor één bit informatie), geen hergebruik van `scheduled_at` (semantisch verkeerd — kloktrigger ≠ business-trigger). Orthogonaal met `depends_on` (kaart-DAG) en `scheduled_at` (klok); `_is_gated` past in dezelfde filter-trits als `_is_due` / `_awaiting_plan_ref`. Canonieke set/clear: `mcp.set_card_gate` + REST `POST /cards/{cid}/set-gate`, met `**Gate:** …`-audit-comment in de activity-feed. | [`kanban-conventions.md` §3a](./kanban-conventions.md) | `f8ef71a0…` |
| 2026-07-15 | Wat wordt "bekijken & overnemen" als een gedispatchte sessie geen tmux-pane heeft — input-streaming of tmux behouden? | **tmux blijft de interactieve transport; takeover = promotie.** Geen input-streaming-UX en geen categorie "human-takeover-kaarten": een headless run wordt op afroep via `claude --resume <session_id>` gepromoveerd tot een echte, attachbare pane mét historie (gemeten). De transport-keuze verschuift van dispatch-tijd naar takeover-tijd. | [`human-takeover-headless-decision.md`](./human-takeover-headless-decision.md) | `80c812af…` |
| 2026-07-15 | Waar staat een gedecomponeerde analyse, en hoe zie je dat haar vervolgtaken landen? | **Parkeerkolom `Awaiting Subtasks` + subtaak-rollup + vijf-statussenvocabulaire.** Een parent met ≥1 kind-kaart (parent-generiek, niet analyse-specifiek) gaat op de Done-move naar `Awaiting Subtasks` i.p.v. `Done`, en sluit automatisch zodra het laatste kind `Done` haalt. De bestaande `parent_card_id`-relatie wordt zichtbaar gemaakt op de parent, met een statusbadge per kind. `ReadyState` gaat van 3 → 5: `blocked`→`dependent` (wacht op andere kaarten — DAG of kinderen), `dispatching`→`in_progress`, plus nieuw `impeded` + `completed`. `completed` is **afgeleid** (`column == "Done"`), géén opgeslagen label — anders dan `not-feasible`/`no-action-needed`, die informatie dragen die de kolom niet heeft. Complementair aan de uitkomst-poort hieronder: die beslist *of* een analyse mag afsluiten, dit beslist *waarheen*. | [`analyse-levenscyclus-decision.md`](./analyse-levenscyclus-decision.md) | `d0089809…` |
| 2026-07-15 | Hoe krijgt de analyse-fase een afdwingbaar gevolg (analyses gaan naar Done zonder resultaat)? | **Uitkomst-poort op de Done-move.** Een analyse-kaart mag Done alleen binnen met een expliciete `outcome` uit een gesloten enum (`decomposed` — geverifieerd tegen echte kind-kaarten / `not_feasible` → label / `no_action_needed`); "input nodig" blijft `report_impediment`. Prompt-instructie alleen is afgewezen — dat was de vorige twee rondes en niets verifieerde het. | [`analysis-outcome-contract-decision.md`](./analysis-outcome-contract-decision.md) | `e95729bb…` |
| 2026-07-14 | Meerdere accounts binnen één vendor (§7-fork abonnement-flexibiliteit)? | **NO-GO — vendor-divers.** Subscription-identiteit blijft `{cli, provider}`; de same-vendor-spike is afgesloten, C1–C4 niet geopend. | [`spike-same-vendor-multi-account-isolation.md`](./spike-same-vendor-multi-account-isolation.md), [`subscription-flexibiliteit-analyse.md`](./subscription-flexibiliteit-analyse.md) §7 | `290f6fb7…` |
| 2026-07-14 | ACP of een per-CLI stream-json-parser als gestructureerd transport achter `SpawnTransport`? | **Conditionele GO op gestructureerd transport, NO-GO op ACP als eerste.** Eerste slice met `claude -p --output-format stream-json`; event-model ACP-isomorf; ACP-adapter gepoort op tweede-provider-onboarding. | [`acp-transport-decision.md`](./acp-transport-decision.md) | zie doc |
| 2026-07-14 | Synchrone in-sessie-subagent vs. async kanban-kind-kaart — welk model wint? | **Complementair, niet concurrerend.** Grens op één as: durability + bordzichtbaarheid vs. gedeelde in-memory context. Async-decompositie blijft één laag diep aan de bordkant. | [`sync-vs-async-delegation-decision.md`](./sync-vs-async-delegation-decision.md) | zie doc |
| 2026-07-17 | Plans-pagina is leeg — oplappen of herbestemmen? | ✅ **Optie B — herbestemmen.** Plans wordt read-only mensvenster op de spec-/plan-laag (B = kaart-plan-attachments + C = `docs/cockpit/`-docs); `kanban_plans` wordt uitgefaseerd. Randvoorwaarde gebruiker: de B↔C-join via `spec_doc` is 0× gepopuleerd → lever B en C éérst náást elkaar, join is uitgesteld werk. Gedecomponeerd in 4 vervolgkaarten (aggregator-backend → frontend → uitfaseren; + uitgestelde join-analyse). A (volledig uitfaseren) en C (writer aanhaken) afgewezen. _(Tot 2026-07-15 stond dit ten onrechte als beslist door backfill-fout `4101d56`; review corrigeerde naar "nog niet beslist"; dit is de eerste échte go.)_ | [`plans-feature-decision.md`](./plans-feature-decision.md) §7, §10 | `45ac606e…` (review: `a70a9272…`) |
| 2026-07-17 | Per-kaart run-ledger (orchestrator-scherm §3.2) — nieuw scherm of `CardDrawer`-uitbreiding, en `structured_events` consumeren of bestaande bronnen aggregeren? | ✅ **Bouwen, als aggregatie in een `CardDrawer`-tab.** Nieuw top-level-scherm afgewezen (dupliceert kaart-navigatie) → `Ledger`-tab naast Deliverables/Activity/Plan/Tokens/Run. `structured_events` als primaire bron **NO-GO nu** — dubbel geblokkeerd: headless is niet default (`DEFAULT_TRANSPORT="worktree"`) én `headless_runner._on_event` gooit events weg (geen store). Fase 1 = stitch bestaande durabele bronnen (git-diffstat + activity-outcome + verify/CI + per-model tokens) tot de spine `prompt → files → tests → outcome → model`; linkt naar bestaande Run/Tokens-tabs i.p.v. te dupliceren (geen overlap met CC Bridge/APM/TokensTab). `structured_events`-timeline = fase-2-verrijking, wordt kaart zodra headless een gebruikt pad is. Gedecomponeerd in 2 vervolgkaarten (backend-aggregator → frontend Ledger-tab). | [`run-ledger-decision.md`](./run-ledger-decision.md) | `4ce329cd…` |
| 2026-07-14 | Welke tool voert het intake-interview (vrij gesprek → ingevulde intake-kaart)? | **`superpowers:brainstorming` + `writing-plans`** in een dunne `intake-authoring`-skill. Niet spec-kit (zware dep, dubbele orkestratie). `intake_kind` vervalt voor de MVP. | [`intake-authoring-flow-decision.md`](./intake-authoring-flow-decision.md) | `f2fe8548…` |
| 2026-07-14 | `intake_kind` nu toevoegen, of YAGNI? | **Geen standalone veld nu** (geen enum zonder lezer), maar het echte gemis — de interview-/intake-authoring-flow — krijgt één `analysis`-vervolgkaart waarin `intake_kind` mét consument meekomt. ↩︎ afgesloten door `intake-authoring-flow-decision.md`. | [`intake-kind-decision.md`](./intake-kind-decision.md) | `646f5860…` |
| 2026-07-11 | Database-plafond — blijven op SQLite of naar Postgres? | **Blijven op SQLite.** Nu goedkoop hardenen (één metric toevoegen), niet migreren. | [`database-scaling-decision.md`](./database-scaling-decision.md) | zie doc |
| 2026-07-11 | Schema-migraties — `create_all` + handmatige renames of Alembic? | **Alembic invoeren**, forward-only en SQLite-first. | [`schema-migrations-decision.md`](./schema-migrations-decision.md) | zie doc |
| 2026-07-11 | Orchestratie-substraat — tmux + CLI-scraping vs. Claude Agent SDK / headless? | **Incrementeel abstraheren** — niet migreren en niet bevriezen. Headless/gestructureerd transport náást tmux; tmux blijft default voor human-in-the-loop. | [`orchestration-substrate-decision.md`](./orchestration-substrate-decision.md) | zie doc |
| 2026-07-11 | Headless `SessionEnd`-retro voor niet-gedispatchte sessies bouwen? | **Niet bouwen.** In plaats daarvan de bestaande in-session retro uitbreiden naar álle gedispatchte sessies — concreet: het analyst-gat sluiten. | [`headless-session-retro-decision.md`](./headless-session-retro-decision.md) | zie doc |
| 2026-07-11 | Spec-driven development fase 0 — minimale index of structurele consolidatie? | **Optie B (maximalistisch).** `docs/plans/` gearchiveerd → `docs/plans-legacy/`; `docs/cockpit/` expliciet én afdwingbaar canoniek; promotie-ledger + check-script. | [`spec-driven-development-fase-0-decision.md`](./spec-driven-development-fase-0-decision.md) | zie doc |
| 2026-07-10 | Reviewer-agent + Review-kolom — wenselijk? | **Wél bouwen, in lichtere vorm** (REVISED): feature-compliance-review als subagent-call binnen dezelfde engineer-sessie vóór `move_card Done`. Geen aparte persona, geen Review-kolom. | [`reviewer-agent-decision.md`](./reviewer-agent-decision.md) | zie doc |
| 2026-07-10 | Kan een completed beslissing weerlegd + heropend worden mét context? | De twee bouwstenen die "genoeg context" leveren bestaan al; aanbeveling incl. een `reopen_card`-achtige tool zodat ook een agent een beslissing kan heropenen. | [`reopen-completed-decision-analysis.md`](./reopen-completed-decision-analysis.md) | zie doc |
| 2026-07-09 | Past de `updates` (self-update) feature nog bij Cockpit's missie? | **Houden, zoals het is.** Geen aanpassing aan `scripts/update.sh`, router of page. | [`updates-feature-decision.md`](./updates-feature-decision.md) | zie doc |
| 2026-07-09 | Upstream verwijderde Docker-support — overnemen? | **Niet overnemen.** Cockpit blijft bij Docker als primaire/aanbevolen flow. | [`upstream-docker-removal-decision.md`](./upstream-docker-removal-decision.md) | zie doc |
| 2026-07-09 | Welke cadans voor het terugkerende zelfverbeteringsonderzoek? | Voorstel voor een terugkerende cadans bovenop de bestaande scheduling-infrastructuur. | [`recurring-cadence-proposal.md`](./recurring-cadence-proposal.md) | zie doc |
| 2026-07-08 | Upstream verwijderde legacy Presence — overnemen? | **Niet overnemen.** Presence blijft in Cockpit staan zoals het is. | [`upstream-presence-removal-decision.md`](./upstream-presence-removal-decision.md) | zie doc |
| 2026-07-08 | Upstream Agent Team Presets / launch-orchestration adopteren? | **Niet adopteren** — concurrerend orchestratie-paradigma naast kanban-dispatch, geen complement. Alleen de universele provider-correctness-bugs zijn gecherrypickt. | [`upstream-agent-teams-decision.md`](./upstream-agent-teams-decision.md), [`kanban-followups.md`](./kanban-followups.md) | zie doc |
| 2026-07-04 | Model-switching binnen Claude Code — bouwen of integreren? | Decided (build-vs-integrate); implementatie niet gestart. | [`spike-claude-code-model-switching.md`](./spike-claude-code-model-switching.md) | zie doc |
| 2026-07-03 | Declaratieve workflow-orchestration — bouwen of integreren? | Decided (build-vs-integrate); implementatie niet gestart. | [`spike-declarative-workflow-orchestration.md`](./spike-declarative-workflow-orchestration.md) | zie doc |
| 2026-06-18 | Sync-laag — bevriezen of snoeien? | **Optie 3: snoei het dode, bevries de kern.** `sync.py` + tests verwijderd; HLC/op-log/LWW blijven als gedocumenteerde dormante kern. | [`sync-hlc-freeze-vs-prune.md`](./sync-hlc-freeze-vs-prune.md) | zie doc |

## Conventie — een beslissing landt hier

Een `[beslissing]`-kaart (of elke analyse-kaart waarvan de deliverable een
richtingsbeslissing is) is **pas klaar** als deze drie dingen kloppen:

1. **Het beslisdocument bestaat** — `docs/cockpit/<onderwerp>-decision.md`, met bovenaan
   een quote-blok dat de kanban-kaart (titel + id) noemt. Dit is waar de redenering leeft.
2. **Er staat een regel in dit register** — bovenaan de tabel (nieuwste eerst), met
   datum, vraag, uitkomst-in-één-zin, doc-link en kaart-id. Link-only: kopieer de
   redenering niet.
3. **De Done-summary van de kaart verwijst naar het document**, niet andersom. De
   summary is vluchtig; het document is canoniek.

Herzie je een bestaande beslissing? Werk het bron-document bij, voeg een **nieuwe**
regel bovenaan toe en markeer de oude regel met `↩︎ herzien door <link>` — zo blijft de
chronologie leesbaar en verdwijnt er geen historie.

### Hoe nieuwe regels worden ingevoegd — append-friendly via `merge=union`

Omdat **elke** nieuwe beslissings-regel op exact dezelfde positie wordt ingevoegd
(direct onder de `|---|---|---|---|` header van de tabel — er is maar één "nieuwste
eerst"-plek), zou een conventionele three-way merge bij twee gelijktijdige inserts
op een `CONFLICT (content)` in dit bestand uitkomen. Een haastige `git checkout
--ours` zou dan een regel uit de index laten verdwijnen — precies wat dit register
probeert te voorkomen.

Daarom draagt `docs/cockpit/decisions.md` in `.gitattributes` de
[`merge=union`](https://git-scm.com/docs/gitattributes#_defining_merge_attributes)
strategie:

```gitattributes
docs/cockpit/decisions.md merge=union
```

`merge=union` is een file-level merge: bij een conflict houdt git de **unie van
beide kanten' toegevoegde regels**, in plaats van een regel-tegen-regel-resolutie.
Een insert van sessie A en een insert van sessie B op dezelfde positie resulteren
daardoor in **beide rijen in het merge-resultaat**; de volgorde tussen twee
gelijktijdige inserts is niet-deterministisch, maar de `Datum`-kolom maakt de
chronologie expliciet — daar wijkt niets van af.

**Wat dit betekent voor jou als auteur van een nieuwe regel:**

- Schrijf je rij zoals je altijd zou doen: op de "nieuwste eerst"-plek, direct onder
  de header.
- **Forceer geen rebase / re-sort** van bestaande rijen om de tabel "mooi" te
  houden — dat is precies wat de volgende sessie weer ongedaan maakt, en het
  introduceert geen waarde die de `Datum`-kolom niet al biedt.
- Draai de `.gitattributes`-regel niet terug. Als je denkt dat 'ie weg mag:
  lees eerst kanban-kaart `16ce4d89…` (de merge-conflict-incident die 'm
  rechtvaardigt) en de acceptatiecriteria daarvan.

**Wat dit NIET doet:**

- Het verandert de **leesconventie** niet — het register blijft "nieuwste eerst"
  in de geest (de `Datum`-kolom is canonical, niet de regelvolgorde).
- Het beschermt niet tegen edits aan *dezelfde* regel door twee sessies
  (union houdt beide varianten, geen semantische merge) — een regel is append-only,
  bestaande rijen worden niet herzien in dit register (zie de "↩︎ herzien door"-regel
  hierboven voor revisies).

### Header-conventie — wat bovenaan elk `*-decision.md` hoort te staan

Elk beslisdocument begint — direct onder de `# Titel`-regel, vóór de eerste
`##`-sectie of `>`-blokquote — met dit vier-velden-header:

```markdown
**Datum:** YYYY-MM-DD
**Status:** besloten | herzien | voorgesteld
**Kaart:** `<card-id>`
**Uitkomst:** <één zin — dezelfde zin als de register-regel>
```

| Veld | Bron | Verplicht? |
|---|---|---|
| **Datum** | `git log --reverse --format=%ad --date=short -- <doc>` wanneer niet in het doc | ja |
| **Status** | Canonical waarden `besloten` / `herzien` / `voorgesteld`; vrije toevoeging tussen haakjes blijft toegestaan | ja |
| **Kaart** | `<card-id>` in backticks wanneer beschikbaar; anders `_zie doc — geen hex-id in dit beslisdoc vastgelegd_` (placeholder, refactor-TODO) | ja |
| **Uitkomst** | Eerste zin van de overeenkomstige register-rij, verbatim. Voor `plans-feature-decision.md` (`⏳ NOG NIET BESLIST`) blijft die tekst expliciet staan. | ja |

**Backfill-volgorde** bij het aanmaken van een nieuw beslisdoc: trek `Datum` uit
`git log --reverse`, kopieer `Uitkomst` van de register-rij die je zojuist
hebt aangemaakt, zet `Status: besloten` (of `herzien` als de revisieteller
hoger is), en vul `Kaart` met de hex-id als die bekend is.

**Validatie:** `bash scripts/check-decision-register.sh --check-headers`
controleert dat alle vier de velden bestaan én dat `**Uitkomst:**` (na
whitespace-normalisatie) een prefix is van de register-rij. Advies-only;
`--strict` geeft exit 1 voor CI-gebruik. De validatie staat los van de
bestaande link-presence-check — beide klassen worden door dezelfde run
geflagd.

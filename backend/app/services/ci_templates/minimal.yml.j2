name: Hello

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Say hi
        run: echo "Hello from a CI-template-generated workflow."
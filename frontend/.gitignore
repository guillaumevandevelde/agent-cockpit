# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Playwright
/test-results/
/playwright-report/

# Istanbul coverage report (generated by `npm run test:coverage`, also in CI).
# The root .gitignore only covers Python's `.coverage`, so 17 generated files
# were tracked here until 2026-08-06 — one of them tripped a high-severity
# js/xss-through-dom CodeQL alert on vendored istanbul JS.
coverage/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
